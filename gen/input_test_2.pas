program test(input, output);
var a : integer;
    b : real;
	c, d : integer;
	e, f : Boolean;
{comment part
}
begin
	a := 2;
	b := 2.0;
	if (a >= 2) then c := 0
	else d := 1; //comment part
	if b = 1.0 then b := b + 1;
	while (a=1) AND (e OR true) do
	begin
		c := c DIV 2;
		d := e MOD 3
	end
end.